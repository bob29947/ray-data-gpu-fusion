# AWS provider dependencies used by the proven sibling G6 harness.
# Install this file in the local Ray CLI environment before --execute; the
# cluster template installs it into the pinned RAPIDS environment on every node.
boto3==1.42.90
botocore==1.42.90
